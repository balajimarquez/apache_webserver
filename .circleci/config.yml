version: 2.1
jobs:
  nginx:          
    docker:
      - image: circleci/node:14  
    steps:
      - checkout
      - setup_remote_docker
      - run: 
          name: Install Tools & Login to AKS
          command: |
            sudo apt-get update
            curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
            sudo apt-get install docker
      - run:
          name: Build Docker Image & Push to ACR
          command: |
            docker build -t $REGISTRY_NAME.azurecr.io/nginx-image -f Dockerfile.nginx .
            az acr login --name $REGISTRY_NAME --username $APP_ID --password $APP_PASSWORD
            docker push $REGISTRY_NAME.azurecr.io/nginx-image:latest


workflows:
  nginx-and-apache:
    jobs:
      - nginx